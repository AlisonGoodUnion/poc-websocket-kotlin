spring:
  main:
    allow-bean-definition-overriding: true
    banner-mode: "off"
  redis:
    host: localhost
    port: 6379
    password: RURAL03
    sentinel:
      master: redis-master
      nodes:
        - localhost
    lettuce:
      shutdown-timeout: 200ms
